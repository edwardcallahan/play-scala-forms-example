# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.WidgetController.index

# WidgetSizing
GET     /widgetsize                 controllers.WidgetController.listWidgetSizes
POST    /widgetsize                 controllers.WidgetController.selectWidgetSize

# Monitor Process
GET     /monitor                    controllers.WidgetController.monitorWidgetCreation

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
